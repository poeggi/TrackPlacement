name: CI/CD - TrackPlacement Release
on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version_override:
        description: 'Version Number (e.g., 1.21.0)'
        required: false
      changelog_override:
        description: 'Manual Changelog (Optional)'
        required: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create Release Archive
        run: |
          zip -r TrackPlacement.mcpack . -x "*.git*" -x "./.github/*" -x "./README.md" -x "./.gitignore"

      - name: Upload to CurseForge
        if: github.event_name == 'release' || github.event.inputs.version_override != ''
        continue-on-error: true
        uses: itsmeow/curseforge-upload@v3
        with:
          token: ${{ secrets.CURSEFORGE_TOKEN }}
          project_id: "1483473"
          game_endpoint: "minecraft-bedrock"
          file_path: "TrackPlacement.mcpack"
          display_name: "TrackPlacement ${{ github.event.inputs.version_override || github.event.release.tag_name }}"
          release_type: "release"
          game_versions: "1.21,26.0,26.3"
